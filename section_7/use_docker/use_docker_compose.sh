# crate architecture based on docker-compose.yml
docker-compose up

# list all services running with docker-compose
docker-compose ps

# drop docker-compose architecture with volums (-v flag)
docker-compose rm -v